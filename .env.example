# Replace with your DB credentials and Valkey URL
DATABASE_URL=mysql://user:password@host:port/db-name
VALKEY_URL=redis://password@host:port
# Number of trusted reverse proxies that append X-Forwarded-For. Keep at 0 for direct traffic.
TRUST_PROXY_HOPS=0
