{
	"$schema": "https://turbo.build/schema.json",
	"globalPassThroughEnv": [
		"API_URL",
		"CORS_ORIGINS",
		"DATABASE_URL",
		"HOST",
		"PORT",
		"REDIS_URL",
		"TRUST_PROXY_HOPS"
	],
	"tasks": {
		"build": {
			"dependsOn": ["^build"]
		},
		"check": {
			"dependsOn": ["^build"]
		},
		"dev": {
			"cache": false,
			"dependsOn": ["^build"],
			"persistent": true
		},
		"test": {
			"dependsOn": ["^build"]
		}
	}
}
