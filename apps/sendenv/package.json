{
	"name": "@sendenv/sendenv",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "NODE_ENV=development PORT=3001 API_URL=http://localhost:3000 bun --watch server.ts",
		"start": "NODE_ENV=production bun server.ts",
		"build": "bun run check",
		"check": "tsc --noEmit -p tsconfig.json",
		"test": "bun test"
	},
	"dependencies": {
		"@sendenv/sdk": "workspace:*",
		"@sendenv/web-ui": "workspace:*",
		"remix": "3.0.0-rc.1"
	}
}
